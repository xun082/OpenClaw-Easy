---
description: 在本仓库中进行代码修改时，要求检查 ESLint 和 TypeScript 类型错误，并禁止自动生成新的 Markdown 文档文件（.md / .mdx 等），除非用户明确要求。
alwaysApply: true
---

# Lint 与 Markdown 相关全局规则

本规则适用于整个仓库，所有 Agent 在执行涉及代码改动的任务时都应遵守。

## 1. 修改 TypeScript / TSX 代码后的检查要求

当对 TypeScript 或 TSX 文件进行非微小修改时（增加组件、调整类型、修改业务逻辑等），应遵循以下要求：

1. 检查 ESLint：
   - 优先使用 Cursor 内置的 Lint 诊断能力，仅对当前修改的文件进行检查。
   - 如需运行脚本，可使用项目内已有脚本（例如 pnpm lint 或子包内的 lint 脚本），但要注意执行范围不要过大，以免浪费时间。
2. 检查 TypeScript 类型：
   - 依赖编辑器 / 语言服务的类型诊断，确保没有新增类型错误。
   - 如项目提供 typecheck 或包含类型检查的 lint 脚本，可以在必要时运行对应脚本。
3. 如因环境限制无法运行脚本：
   - 至少使用 Lint 诊断工具检查当前修改的文件。
   - 在回复用户时说明未能完成完整的脚本级检查，提醒用户本地再运行一次 lint / typecheck 命令。

对于只改注释、字符串文案等“极小变更”，在不影响类型和逻辑的前提下，可以酌情不跑完整脚本，但仍要留意编辑器给出的 Lint / 类型提示。

## 2. 禁止自动创建新的 Markdown 文档文件

默认情况下，Agent 不得在仓库中创建新的 Markdown 文档文件，包括：

- 新建 README.md、CHANGELOG.md 等
- 新建任意以 .md、.markdown、.mdx 结尾的文件

例外情况（允许创建）仅限：

1. 用户在当前对话中明确提出要创建具体的 Markdown 文件，并认可文件名与路径。
2. Cursor 自身配置需要的文件，例如：
   - .cursor/AGENTS.md
   - .cursor/skills/*/SKILL.md
   - .cursor/rules/*.mdc（注意为 .mdc 后缀）

如果用户只说“帮我写个说明”而没有指定要落盘为 .md 文件，Agent 应优先：

- 在对话中直接给出 Markdown 或纯文本内容，而不是写入仓库。

## 3. 修改已有 Markdown 文件时的注意事项

当用户明确要求修改已有的 Markdown 文件时：

1. 只修改与当前任务直接相关的内容，避免大范围重写。
2. 不主动新增全新的 Markdown 文件，而是在用户指定的现有文件内进行补充或修正。

